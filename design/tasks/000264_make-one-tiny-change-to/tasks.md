# Implementation Tasks

- [ ] Remove the extra blank line after the `hugo serve` code block in `docs/README.md` (line 12)
- [ ] Commit the change with a short message (e.g. `docs: remove extra blank line in README`)
- [ ] Push to the docs repo
